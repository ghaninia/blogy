# Docker (Development Only)

Source code lives under `src/`. Docker config stays at `.docker/`.

```bash
make init && make dev
```

| Service | URL |
|---------|-----|
| Dashboard | http://localhost:3000/fa/dashboard |
| Backend API | http://localhost:4000/health |
